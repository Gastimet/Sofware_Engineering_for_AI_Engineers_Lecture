pipeline {
    agent any

    environment {
        PROJECT_DIR = 'Autogluon_multiple_regression_models'
    }

    stages {
        stage('Checkout Code') {
            steps {
                echo '📦 Kod, Git reposundan çekiliyor...'
                checkout scm
            }
        }

        stage('Build Trainer Image') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🔨 Docker imajı inşa ediliyor...'
                        // HIZLANDIRMA: --no-cache parametresi KALDIRILDI.
                        // Artık değişiklik yoksa build işlemi saniyeler sürecek.
                        sh 'docker compose build trainer'
                    }
                }
            }
        }

        stage('LLM Vulnerability Scan (Garak)') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🕵️ Garak LLM Vulnerability Scanner çalıştırılıyor...'
                        sh 'docker compose run --rm -T --no-deps --entrypoint "python" trainer -m garak --model_type test.Blank --probes test.Blank --report_prefix garak_report'
                    }
                }
            }
        }

        stage('Start MLflow') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🚀 MLflow servisi başlatılıyor...'
                        sh 'docker compose up -d mlflow'
                        sh 'sleep 15'
                    }
                }
            }
        }

        stage('Run Training & Fairness Check') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🤖 Model eğitimi ve Fairlearn analizi başlatılıyor...'
                        sh 'docker compose run --rm -u root trainer'
                    }
                }
            }
        }

        stage('Giskard Quality Scan') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🕵️ Giskard güvenlik ve kalite taraması yapılıyor...'
                        // DÜZELTME: Dosya yolunun başına klasör adı eklendi.
                        // Konteyner içinde path: /workspace/Autogluon_multiple_regression_models/src/...
                        sh 'docker compose run --rm -u root --entrypoint python trainer Autogluon_multiple_regression_models/src/tests/giskard_scan.py'
                    }
                }
            }
        }
    }

    post {
        always {
            dir(env.PROJECT_DIR) {
                script {
                    echo '🧹 Temizlik yapılıyor...'
                    sh 'docker compose down -v || true'
                }
            }
            cleanWs()
        }
    }
}