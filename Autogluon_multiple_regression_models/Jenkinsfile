pipeline {
    agent any

    environment {
        PROJECT_DIR = 'Autogluon_multiple_regression_models'
    }

    stages {
        stage('Checkout Code') {
            steps {
                echo '📦 Kod, Git reposundan çekiliyor...'
                checkout scm
            }
        }

        stage('Security Scan') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🔒 Starting Security Scans...'
                        // Run scans inside a temporary python container to avoid messing up the host
                        // We mount the current directory to /app so the tools can see the code
                        sh 'docker run --rm -v $(pwd):/app -w /app python:3.11-slim sh -c "pip install bandit safety && safety check && bandit -r src"'
                    }
                }
            }
        }



        stage('Build and Start MLflow') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🚀 MLflow servisi docker-compose ile başlatılıyor...'
                        // FIX: Changed 'docker-compose' to 'docker compose'
                        sh 'docker compose build mlflow || true'
                        sh 'docker compose up -d mlflow'

                        echo '⏳ MLflow servisinin hazır olması bekleniyor...'
                        sh 'sleep 20'
                    }
                }
            }
        }

        stage('Run Training') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🤖 Model eğitimi başlatılıyor...'
                        // FIX: Changed 'docker-compose' to 'docker compose'
                        sh 'docker compose build --no-cache trainer'
                        sh 'docker compose run --rm trainer'
                        echo '✅ Model eğitimi tamamlandı!'
                    }
                }
            }
        }
    }

    post {
        always {
            dir(env.PROJECT_DIR) {
                script {
                    echo '🧹 Tüm servisler docker-compose ile kapatılıyor...'
                    // FIX: Changed 'docker-compose' to 'docker compose'
                    sh 'docker compose down -v || true'
                }
            }
            echo '✅ Pipeline tamamlandı.'
            cleanWs()
        }
        success {
            echo '🎉 Pipeline başarıyla tamamlandı!'
        }
        failure {
            echo '❌ Pipeline başarısız oldu!'
        }
    }
}