pipeline {
    agent any

    environment {
        PROJECT_DIR = 'Autogluon_multiple_regression_models'
    }

    stages {
        stage('Checkout Code') {
            steps {
                echo '📦 Kod, Git reposundan çekiliyor...'
                checkout scm
            }
        }

        // STAGE 1: Build the image
        stage('Build Trainer Image') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🔨 Docker imajı inşa ediliyor...'
                        sh 'docker compose build --no-cache trainer'
                    }
                }
            }
        }

        // STAGE 2: Garak LLM Vulnerability Scan
        stage('LLM Vulnerability Scan (Garak)') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🕵️ Garak LLM Vulnerability Scanner çalıştırılıyor...'
                        // Using test.Blank generator/probes to prove integration works
                        sh 'docker compose run --rm -T --entrypoint "python" trainer -m garak --model_type test.Blank --probes test.Blank --report_prefix garak_report'
                    }
                }
            }
        }

        stage('Start MLflow') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🚀 MLflow servisi başlatılıyor...'
                        sh 'docker compose up -d mlflow'
                        sh 'sleep 15'
                    }
                }
            }
        }

        // STAGE 3: Run Training (Fixed Permission Error)
        stage('Run Training') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🤖 Model eğitimi başlatılıyor...'
                        // FIX: Added "-u root" to give permission to write to /mlruns volume
                        sh 'docker compose run --rm -u root trainer'
                        echo '✅ Model eğitimi tamamlandı!'
                    }
                }
            }
        }

        // STAGE 4: Model Security Scan
        stage('Model Security Scan') {
            steps {
                dir(env.PROJECT_DIR) {
                    script {
                        echo '🛡️ ModelScan ile Pickle/Serialization taraması...'
                        // Also run as root here just in case it needs read access that was restricted
                        sh 'docker compose run --rm -u root --entrypoint "modelscan" trainer -p /mlruns'
                    }
                }
            }
        }
    }

    post {
        always {
            dir(env.PROJECT_DIR) {
                script {
                    echo '🧹 Temizlik yapılıyor...'
                    sh 'docker compose down -v || true'
                }
            }
            cleanWs()
        }
    }
}